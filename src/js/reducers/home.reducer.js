/**
 * Created by chenkang1 on 2017/6/3.
 */

const initialState = {
    type:'nav'
};

const home = (state = initialState, action) => {

    switch (action.type) {

        default:
            return state;
    }
};

export default home;
